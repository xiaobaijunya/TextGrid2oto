#wav路径（必改）
wav_path=F3
#字符分隔符
cut=_,-
#忽略ds音素
ignore=AP,SP
#是否生成lab,y or Y 生成，其他不生成
lab=Y
#TextGrid路径（必改）
TextGrid_path=F3/TextGrid
#sofa模型对应ds字典路径
ds_dict=opencpop-extension.txt
#presamp.ini的路径（必改）
presamp=presamp.ini
#音阶后缀
pitch=
#是否覆盖oto，Y or y覆盖，其他不覆盖
cover=n
#下面两个参数都是除法，2=占比1/2
#固定&右线：预发声→右线的长度的占比。（cv：元音占比，VC：辅音占比）
#交叉:预发声长度的占比。（cv：辅音占比，VC：元音占比）
#-CV和CV规则：左线占比,固定的占比,右线占比,预发声不变,交叉占比
cv_sum=1,3,1.5,1,2
#VC和VV规则：左线占比,固定的占比,右线占比,预发声不变,交叉占比,VV的固定占比
vc_sum=3,0,2,1,2,3


